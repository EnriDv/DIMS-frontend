chmod +x .husky/pre-commit

. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged
npx lint-staged

if [ $? -ne 0 ]; then
  echo "❌ Lint-staged failed. Fix the errors above."
  exit 1
fi

# Type check

#echo "✅ Type checking..."
#npm run type-check

#if [ $? -ne 0 ]; then
#  echo "❌ Type check failed. Fix the errors above."
#  exit 1
#fi

echo "✨ Pre-commit checks passed!"